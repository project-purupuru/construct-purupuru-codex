# Dependencies
node_modules/

# Build output
dist/

# Runtime state
.run/

# LOA SUBMODULE SYMLINKS
# These symlinks point into .loa/ submodule — tracked so CI can resolve them
.claude/settings.json
.claude/checksums.json
.loa-state/
.claude.backup.*
